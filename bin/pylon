#!/bin/bash
#Loader script to execute the python linker, Pylon

#get directory that this script lives at
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
FILE="$DIR/src/pylon.py"
python3.6 $FILE $@
