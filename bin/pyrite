#!/bin/bash
#Loader script to execute the python rewriter, Pyrite

#get directory that this script lives at
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
FILE="$DIR/src/pyrite.py"
python3.6 $FILE $@
