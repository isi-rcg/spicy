#!/bin/bash
#Loader script to execute the Pyramid tool to generate scripts to driver SDSoC

#get directory that this script lives at
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
FILE="$DIR/src/pyramid.py"
python3.6 $FILE $@
